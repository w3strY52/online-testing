export type TUserAuth = {
    id: number,
    login: string,
    firstName: string,
    secondName: string,
    lastName: string,
    form: string
};

export type TAuth = {
    login: '',
    password: ''
};
